/** Foco alinhado à paleta (--brand-sky-700 / --focus-ring em globals.css). */
export const focusRingBrandClass =
  'outline-none transition focus:border-brand-accent focus:shadow-[var(--focus-ring)]';
